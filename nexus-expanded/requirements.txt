# NEXUS v4.0 requirements
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
httpx==0.25.1
requests==2.31.0

# Optional — install only what your hardware needs
# pynvml          # NVIDIA GPU telemetry (pip install pynvml)
# pysnmp          # SNMP if not using snmptrap CLI
