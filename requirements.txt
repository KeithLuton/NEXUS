# Runtime dependency: optional. If not installed, the orchestrator falls back
# to a deterministic mock sensor so you can develop and test without a BMC.
redfish>=3.0.0

# Test dependency.
pytest>=7.0.0
